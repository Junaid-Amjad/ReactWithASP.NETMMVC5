import { observer } from "mobx-react-lite";
import { Label } from "semantic-ui-react";

export default observer(function mapFloor() {
  return (
    <>
      <Label content="Map Floor" />
    </>
  );
});
